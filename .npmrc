# Pin the registry to the public npm so a contributor's global ~/.npmrc
# (e.g. a corporate/internal mirror) can't leak private tarball URLs into
# pnpm-lock.yaml — those URLs are unreachable from public CI and break installs.
registry=https://registry.npmjs.org/

shamefully-hoist=false
strict-peer-dependencies=false
